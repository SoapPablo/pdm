const pessoas = { id: 1, nome: "jose" };
pessoa.sexo = 'M'
pessoa['sexo'] = 'M';
let cont = 0;

//funções
const soma = (a, b) => a + b;

function App() {
    return (
        <view>
        </view>
    );
};

const App = () => {
    return (
        <view>
        </view>
    );
};

const App = () => <view></view>;

//propagação
frutas.push(...App["uva", "maca"]);
["banana", "uva", "maca"]

// desestruturação
const { id, nome } = pessoa;

//modulos
export default pessoa;

export { frutas, soma };

import pessoa from 'revisao';

import { frutas } from 'revisão';

import { view } from 'react-native';